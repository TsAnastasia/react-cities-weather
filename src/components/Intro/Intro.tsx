import React from "react";
import styles from "./intro.module.scss";

const Intro = () => (
  <section className={styles.intro}>
    <h1>City's weather app</h1>
    <p>Find out weather in your city</p>
  </section>
);

export default Intro;
